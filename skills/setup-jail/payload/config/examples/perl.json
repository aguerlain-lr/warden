{
  "tool": "perl",
  "binaries": [],
  "default": "abstain",
  "rewrite": [
    { "argv": { "contains": ["-e"], "notContains": ["-i"] }, "to": "jail-perl", "reason": "inline perl code auto-boxed (no network, repo read-only)" },
    { "argv": { "contains": ["-E"], "notContains": ["-i"] }, "to": "jail-perl", "reason": "inline perl code auto-boxed (no network, repo read-only)" },
    { "argv": { "scratchScript": true, "notContains": ["-i"] }, "to": "jail-perl", "reason": "scratch perl script auto-boxed (no network, repo read-only)" }
  ]
}
